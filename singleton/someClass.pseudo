public SomeClass{

	public main(){
		Logger l = new Logger();
		l.info("hello world");
	}
}

/** CHANGED CODE / UPDATED CODE **/

public SomeClass{

	public main(){
		Logger l = Logger.getLogger(); /** CHANGE: the new logger 'l' is now instantiated using the getLogger() method and not the new keyword **/
		l.info("hello world");
	}
}
